{
  "packageManager": "yarn@4.3.1",
  "type": "module",
  "dependencies": {
    "json-server": "^1.0.0-beta.1"
  },
  "scripts": {
    "start": "live-server src --mount=/src:./src",
    "server": "json-server --watch './src/data/data.json' --port 3001"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
